import {StyleSheet} from 'react-native';

export const TaskStyle = StyleSheet.create({
  totalTask: {
    backgroundColor: '#E1E1E1',
    padding: 2,
    width: 35,
    borderRadius: 20,
    alignItems: 'center',
    marginLeft: 10,
  },
});
